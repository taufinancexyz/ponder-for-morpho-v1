# Morpho Ponder Template

Directly forked from the [Ponder Template](https://github.com/morpho-org/ponder-for-morpho-v1).

To get started, create .env.local from .env.template and run pnpm run dev inside the `apps/ponder` folder.
